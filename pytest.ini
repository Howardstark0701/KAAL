[pytest]
# Collect only from tests/ so examples/test_fgsm.py cannot collide with
# tests/test_fgsm.py (same basename) during rootdir collection.
testpaths = tests

filterwarnings =
    ignore::DeprecationWarning:pyparsing
    ignore::DeprecationWarning:matplotlib
    ignore::UserWarning:matplotlib
